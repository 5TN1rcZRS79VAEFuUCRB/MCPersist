pluginManagement {
    repositories {
        maven { url "https://maven.fabricmc.net/" }
        gradlePluginPortal()
    }
}

// forge/ holds e4mc's loader glue, not built until it gets its own build (#23).
include("fabric", "neoforge")

rootProject.name = "mcpersist"
