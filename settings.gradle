pluginManagement {
    repositories {
        maven { url "https://maven.fabricmc.net/" }
        gradlePluginPortal()
    }
}

// Built for Fabric on Minecraft 26.x only. forge/ and neoforge/ keep e4mc's loader glue
// for later; they are not part of the build.
include("fabric")

rootProject.name = "mcpersist"
